/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  darkMode: "class", // ← to dodaje tryb ciemny sterowany klasą
  theme: {
    extend: {},
  },
  plugins: [],
};
